import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard | CargoVision",
};

export default function DashboardPage() {
  return (
    <div className="p-6">
      {/* Dashboard home content can be added here in future */}
    </div>
  );
} 